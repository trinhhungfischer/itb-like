# Game World Entity Registry
#
# PURPOSE: Single source of truth for all named game-world facts that appear
# in more than one document. Skills check this before writing new content to
# detect cross-doc inconsistencies at authoring time.
#
# RULES:
#   - Only register facts that cross system boundaries. Internal-only facts
#     (a formula used in one GDD only) do NOT need to be registered.
#   - Never delete entries — set status: deprecated instead.
#   - When a value changes: update the value, set revised: to today's date,
#     and add a comment with the old value and which GDD changed it.
#   - source: is the authoritative GDD — the one that "owns" this fact.
#     Other GDDs that reference it list themselves in referenced_by.
#   - When a new GDD references an existing entry, append its path to
#     referenced_by. Do not create a duplicate entry.
#
# WRITTEN BY: /design-system (Phase 5 — after GDD sections are approved)
#             /consistency-check (when resolving conflicts)
# READ BY:    /design-system (Phase 2 — before authoring begins, and after
#               Section C/D writes to conflict-check new entities/formulas)
#             /consistency-check (primary input — grep-first, GDD-second)
#             /review-all-gdds (Phase 1 — baseline for Phase 2 checks)
#             /architecture-review (data structure and interface validation)
#
# SEARCH PATTERNS (for skills using Grep):
#   All entity names:        Grep pattern="^  - name:" path="design/registry/entities.yaml"
#   Specific entity:         Grep pattern="  - name: goblin" path="design/registry/entities.yaml"
#   All items:               Grep pattern="^  - name:" path="design/registry/entities.yaml" (items section)
#   What combat.md owns:     Grep pattern="source: design/gdd/combat.md"
#   What inventory.md uses:  Grep pattern="referenced_by.*inventory"
#   All gold values:          Grep pattern="value_gold:"
#   Deprecated entries:      Grep pattern="status: deprecated"
#
# FORMAT: YAML. Consistent indentation is critical for grep reliability.
#   Sections: entities | items | formulas | constants
#   Each entry: name, status, source, referenced_by[], attributes{}, added, revised

version: 1
last_updated: "2026-07-28"

# ─── ENTITIES ────────────────────────────────────────────────────────────────
# Named game-world objects: enemies, NPCs, characters, factions, bosses.
# Register an entity here when it appears in more than one GDD — e.g., an
# enemy defined in combat.md that also drops items listed in inventory.md.
#
# Required fields: name, status, source, referenced_by, added
# Attribute fields: any key stats that could appear in another GDD
#   (health, damage, drops, faction, etc.)

entities:
  - name: combat_primitives
    status: active
    source: design/gdd/combat-resolution.md
    referenced_by:
      - design/gdd/combat-resolution.md
      - design/gdd/heroes-and-abilities.md
      - design/gdd/enemy-abilities-and-telegraph.md
      - design/gdd/move-preview.md
      - design/gdd/ability-upgrades.md
      - design/gdd/board-rendering-and-juice.md
      - design/gdd/encounter-generator.md
    note: "Closed vocabulary of 10 effect primitives ALL abilities compile into: damage, push, pull, swap, spawnHazard, applyHazard, removeUnit, setTerrain, spawnUnit, + a shared collision-resolution algorithm (push/pull). No system may assume an 11th without amending combat-resolution.md. 'wall' verb = setTerrain(tile, Blocked/Normal). Enemy emergence + on-death broods = spawnUnit(tile, unitSpec) on a Clear tile. Canonical events: DamageApplied, DisplacementComplete, CollisionResolved, SwapComplete, HazardSpawned, HazardApplied, UnitRemoved, TerrainSet, UnitSpawned. Entry point: resolve(board, effects[]) -> events[]. See design/architecture/cross-system-contracts.md."
    added: 2026-07-27
    revised: ""
  - name: unit_record
    status: pending
    source: design/gdd/heroes-and-abilities.md
    referenced_by:
      - design/gdd/heroes-and-abilities.md
      - design/gdd/enemy-abilities-and-telegraph.md
      - design/gdd/objective-and-win-lose.md
      - design/gdd/ability-upgrades.md
      - design/gdd/draft-and-loadout-meta.md
      - design/gdd/pilots.md
    note: "Canonical per-battle Unit record (owner: Heroes & Abilities): { id, team, archetype, maxHP, currentHP, position(tile), size(=1 v1), abilities[], hazardImmunities[], statusFlags[] }. Referenced (not re-shaped) by Enemy, Objective, Ability Upgrades, Draft/Loadout Meta. hazardImmunities threaded through Combat's hazard call sites. Defined in design/architecture/cross-system-contracts.md #6; heroes-and-abilities.md must publish the authoritative schema."
    added: 2026-07-27
    revised: ""
  - name: pilot_instance
    status: active
    source: design/gdd/pilots.md
    referenced_by:
      - design/gdd/pilots.md
      - design/gdd/draft-and-loadout-meta.md
      - design/gdd/run-persistence.md
      - design/gdd/draft-loadout-ui.md
    note: "Per-run pilot record (owner: Pilots): { id, pilotDefinitionId, level(1..pilot_max_level),
      xp, skills[](length == level), status(Active|Dead) }. Lives in RunState.pilots; assignment is
      held by RosterMember.pilotId (null = AI Core, not an entity). Pilots die PERMANENTLY when their
      mech's Unit ends a battle Removed(Defeated|Fell) — the single exception to
      draft-and-loadout-meta.md Rule 3's non-lethal model, which is scoped to mechs (Rule 3a).
      Pilot skills are restricted to action-slot economy, deployment, and run-level effects; they are
      FORBIDDEN from maxHP/moveRange/hazardImmunities (owned by Passive Modules) and from every
      AbilityDefinition field (owned by Ability Upgrades). Touches no sim-core module."
    added: 2026-07-28
    revised: ""

# Example (remove when first real entry is added):
#
# entities:
#   - name: goblin
#     status: active              # active | deprecated
#     source: design/gdd/combat.md
#     referenced_by:
#       - design/gdd/combat.md
#       - design/gdd/inventory.md
#     health: 40
#     damage: 8
#     drops:
#       - item: goblin_arm        # must match an entry in items section
#         qty: 1
#         drop_rate: 0.8          # 0.0–1.0
#     added: 2026-03-26
#     revised: ""


# ─── ITEMS ───────────────────────────────────────────────────────────────────
# Named collectables, equipment, consumables, crafting materials, currency.
# Register an item when its name or value appears in more than one GDD.
#
# Required fields: name, status, source, referenced_by, added
# Attribute fields: value_gold, weight, stackable, category, and any stat
#   modifiers that another system (e.g., economy, crafting) might reference.

items: []

# Example:
#
# items:
#   - name: goblin_arm
#     status: active
#     source: design/gdd/combat.md   # drop defined in combat GDD
#     referenced_by:
#       - design/gdd/combat.md
#       - design/gdd/inventory.md    # inventory GDD lists its weight/stack rules
#       - design/gdd/economy.md      # economy GDD references its sell price
#     value_gold: 5
#     weight: 1
#     stackable: true
#     category: crafting_material
#     added: 2026-03-26
#     revised: ""


# ─── FORMULAS ────────────────────────────────────────────────────────────────
# Named calculations with defined variables and output ranges.
# Register a formula when its output feeds into another system's input,
# or when another GDD references it by name.
#
# Required fields: name, status, source, referenced_by, variables[],
#   output_range[min, max], added
# Optional: expression (the actual formula), notes

formulas:
  - name: manhattan_distance
    status: active
    source: design/gdd/board-and-grid.md
    referenced_by:
      - design/gdd/board-and-grid.md
    variables:
      - a
      - b
    output_range: [0, 14]
    expression: "|a.col - b.col| + |a.row - b.row|"
    notes: "Grid distance metric (4-directional / Manhattan). Max is W+H-2 (14 on
      the default 8x8 board). Used by movement range, ability targeting, and all
      range checks. Movement/push are orthogonal-only by design."
    added: 2026-07-27
    revised: ""
  - name: mulberry32_prng
    status: active
    source: design/gdd/encounter-generator.md
    referenced_by:
      - design/gdd/encounter-generator.md
      - design/gdd/run-structure-node-map.md
      - design/gdd/draft-and-loadout-meta.md
      - design/gdd/pilots.md
    variables:
      - seed
    output_range: [0, 1]
    expression: "mulberry32(seed) PRNG stream + mix() 32-bit hash combiner"
    notes: "Shared deterministic PRNG for PROCEDURAL GENERATION ONLY (map/encounter/draft),
      NEVER for in-battle resolution (battles stay fully deterministic, no RNG). Each
      consumer supplies its own salt constant as a parameter; the algorithm is identical
      and must NOT be re-derived per document."
    added: 2026-07-27
    revised: ""

# Example:
#
# formulas:
#   - name: damage_formula
#     status: active
#     source: design/gdd/combat.md
#     referenced_by:
#       - design/gdd/combat.md
#       - design/gdd/progression.md   # progression GDD scales attack variable
#     variables:
#       - attack
#       - defense
#       - crit_chance
#       - crit_multiplier
#     output_range: [0, 999]
#     expression: "max(0, attack - defense) * (1 + crit_chance * crit_multiplier)"
#     notes: "Output feeds into health-system damage intake. Min is 0 (armour
#       can absorb all damage). Max is uncapped in formula but tuning knob
#       damage_cap in combat.md clamps at 999."
#     added: 2026-03-26
#     revised: ""


# ─── CONSTANTS ───────────────────────────────────────────────────────────────
# Named numerical values referenced across multiple systems.
# Register a constant when it is defined in one GDD but another GDD must
# agree with it (e.g., gold carry limit defined in economy but checked in
# inventory, or base inventory slots defined in inventory but displayed in HUD).
#
# Required fields: name, status, source, referenced_by, value, unit, added

constants:
  - name: grid_width
    status: active
    source: design/gdd/board-and-grid.md
    referenced_by:
      - design/gdd/board-and-grid.md
      - design/gdd/turn-and-phase-manager.md
    value: 8
    unit: tiles
    added: 2026-07-27
    revised: ""
  - name: grid_height
    status: active
    source: design/gdd/board-and-grid.md
    referenced_by:
      - design/gdd/board-and-grid.md
      - design/gdd/turn-and-phase-manager.md
    value: 8
    unit: tiles
    added: 2026-07-27
    revised: ""
  - name: squad_size
    status: active
    source: design/gdd/heroes-and-abilities.md
    referenced_by:
      - design/gdd/heroes-and-abilities.md
      - design/gdd/turn-and-phase-manager.md
      - design/gdd/draft-and-loadout-meta.md
      - design/gdd/ability-upgrades.md
      - design/gdd/meta-progression-and-unlocks.md
      - design/gdd/map-run-ui.md
      - design/gdd/draft-loadout-ui.md
    value: 3
    unit: heroes
    added: 2026-07-27
    revised: ""
  - name: actions_per_hero_turn
    status: active
    source: design/gdd/heroes-and-abilities.md
    referenced_by:
      - design/gdd/heroes-and-abilities.md
      - design/gdd/turn-and-phase-manager.md
      - design/gdd/ability-upgrades.md
      - design/gdd/encounter-generator.md
    value: 2
    unit: actions
    added: 2026-07-27
    revised: ""
  - name: upgrade_slots_per_hero
    status: active
    source: design/gdd/ability-upgrades.md
    referenced_by:
      - design/gdd/ability-upgrades.md
      - design/gdd/draft-and-loadout-meta.md
      - design/gdd/meta-progression-and-unlocks.md
    value: 2
    unit: slots
    added: 2026-07-27
    revised: ""
  - name: collision_damage
    status: active
    source: design/gdd/combat-resolution.md
    referenced_by:
      - design/gdd/combat-resolution.md
      - design/gdd/heroes-and-abilities.md
      - design/gdd/move-preview.md
    value: 1
    unit: hp
    added: 2026-07-27
    revised: ""
  - name: fire_damage_per_tick
    status: active
    source: design/gdd/combat-resolution.md
    referenced_by:
      - design/gdd/combat-resolution.md
      - design/gdd/enemy-abilities-and-telegraph.md
      - design/gdd/encounter-generator.md
    value: 1
    unit: hp
    added: 2026-07-27
    revised: ""
  - name: pilot_xp_per_battle
    status: active
    source: design/gdd/pilots.md
    referenced_by:
      - design/gdd/pilots.md
    value: 1
    unit: xp
    note: "Awarded at battle_ended to the pilot of each deployed mech whose Unit did NOT end
      Removed. Flat per battle, never per kill (kill-based XP would contradict Pillar 2).
      Awarded regardless of win/loss. Benched pilots earn nothing."
    added: 2026-07-28
    revised: ""
  - name: pilot_level2_xp
    status: active
    source: design/gdd/pilots.md
    referenced_by:
      - design/gdd/pilots.md
    value: 2
    unit: xp
    note: "Cumulative XP threshold for level 2. INVARIANT: pilot_level2_xp < pilot_level3_xp,
      validated at config load — a violation makes level 2 unreachable."
    added: 2026-07-28
    revised: ""
  - name: pilot_level3_xp
    status: active
    source: design/gdd/pilots.md
    referenced_by:
      - design/gdd/pilots.md
    value: 4
    unit: xp
    note: "Cumulative XP threshold for level 3. Tuned against run length: run-structure-node-map.md
      map_depth=6 + Boss yields ~4-5 combat nodes, so a starting pilot peaks as the run reaches its
      Boss. Retune together with pilot_xp_per_battle if map_depth changes."
    added: 2026-07-28
    revised: ""
  - name: pilot_max_level
    status: active
    source: design/gdd/pilots.md
    referenced_by:
      - design/gdd/pilots.md
    value: 3
    unit: levels
    note: "Ceiling on PilotInstance.level and therefore on len(skills) — 1 innate + 2 learned.
      Raising it compounds the five-axis legibility risk (pilots.md Open Question #2) and
      requires a larger skill catalog per Formula F3."
    added: 2026-07-28
    revised: ""
  - name: node_bonus_stack_cap
    status: active
    source: design/gdd/node-bonuses.md
    referenced_by:
      - design/gdd/node-bonuses.md
    value: 3
    unit: stacks
    note: "Maximum counted stacks per node bonus (Formula F2). A run claims map_depth+1 = 7
      nodes total, so this caps route-commitment payoff. At 1 every bonus becomes binary
      (claimed/not) and route DEPTH stops mattering; above 7 it never binds. Retune together
      with map_depth — changing run length invalidates it."
    added: 2026-07-28
    revised: ""
  - name: settings_write_debounce_ms
    status: active
    source: design/gdd/settings-and-options.md
    referenced_by:
      - design/gdd/settings-and-options.md
    value: 500
    unit: milliseconds
    note: "Debounce between a committed settings change and the localStorage write. Bounds
      the loss window if a session ends abruptly. Below ~100ms a slider drag thrashes
      synchronous localStorage on the main thread; above ~2000ms a change made just before
      closing the tab is reliably lost."
    added: 2026-07-28
    revised: ""
  - name: delta_e_min
    status: active
    source: design/gdd/accessibility.md
    referenced_by:
      - design/gdd/accessibility.md
    value: 15
    unit: CIEDE2000
    note: "Minimum perceptual separation between any two palette colors under simulated
      deuteranopia/protanopia/tritanopia (Formula F4). ADVISORY gate, not blocking: shape/icon
      redundancy (Core Rule 2, Formula F3 greyscale test) is the correctness guarantee, and an
      8-hue palette may legitimately fail F4. If it fails consistently the fix is a smaller
      palette, NOT a lower threshold."
    added: 2026-07-28
    revised: ""
  - name: pilot_skill_offer_count
    status: active
    source: design/gdd/pilots.md
    referenced_by:
      - design/gdd/pilots.md
      - design/gdd/draft-loadout-ui.md
    value: 3
    unit: offers
    note: "Skills offered on level-up, drawn seeded via mulberry32 salted with pilot id + level.
      Player must pick exactly 1 — no skip path, unlike a DraftOffer (a level-up is pure gain).
      Catalog must hold at least pilot_skill_offer_count + pilot_max_level entries."
    added: 2026-07-28
    revised: ""

# Example:
#
# constants:
#   - name: gold_carry_limit
#     status: active
#     source: design/gdd/economy.md
#     referenced_by:
#       - design/gdd/economy.md
#       - design/gdd/inventory.md   # inventory enforces the carry limit
#       - design/gdd/ui.md          # HUD displays current gold vs limit
#     value: 9999
#     unit: gold
#     added: 2026-03-26
#     revised: ""
#
#   - name: base_inventory_slots
#     status: active
#     source: design/gdd/inventory.md
#     referenced_by:
#       - design/gdd/inventory.md
#       - design/gdd/progression.md  # progression unlocks additional slots
#     value: 20
#     unit: slots
#     added: 2026-03-26
#     revised: ""
